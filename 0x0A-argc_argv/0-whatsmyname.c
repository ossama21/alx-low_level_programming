#include "main.h"
#include <stdio.h>

/**
 * main - Print the program name
 * @argc: The Number of the command lines arguments
 * @argv: The Array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
